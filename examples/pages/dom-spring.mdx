# Flex Dom Spring

import { FlexDomSpring, FlexDomSpringRoot } from "../components/flex-dom-spring"
import { SliderInput } from "../components/slider-input"
import { FLEX_DIRECTION_ROW, FLEX_DIRECTION_COLUMN } from "yoga-layout-prebuilt"

<SliderInput>
    {(value) => (
        <FlexDomSpringRoot flexDirection={FLEX_DIRECTION_ROW}>
            <FlexDomSpring flexDirection={FLEX_DIRECTION_COLUMN} index={0} flexGrow={value}>
                <FlexDomSpring index={0} flexGrow={value} />
                <FlexDomSpring index={1} flexGrow={1} />
            </FlexDomSpring>
            <FlexDomSpring index={1} flexGrow={1} />
        </FlexDomSpringRoot>
    )}
</SliderInput>

```typescript
<FlexDomSpringRoot flexDirection={FLEX_DIRECTION_ROW}>
    <FlexDomSpring flexDirection={FLEX_DIRECTION_COLUMN} index={0} flexGrow={value}>
        <FlexDomSpring index={0} flexGrow={value}></FlexDomSpring>
        <FlexDomSpring index={1} flexGrow={1} />
    </FlexDomSpring>
    <FlexDomSpring index={1} flexGrow={1} />
</FlexDomSpringRoot>
```
